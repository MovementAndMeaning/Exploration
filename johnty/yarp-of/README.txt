yarp of empty project

expects YARP_DIR and ACE_ROOT to point to correct locations, and YARP libs to be compiled.

should compile when deployed in an oF apps directory using Visual Studio version of oF 0.8

example performs a simple register of a port, nothing more.